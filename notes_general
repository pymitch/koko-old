#general notes:

create sentences from scraping screenplays and literature
organize sentences into difficulty
    display appropriate sentence according to user difficulty

run virtual env:
source ../ENV/bin/activate

run server:
python manage.py runserver

git instructions:
to commit:
git add *
git commit -m "message"
git push

to pull:
git pull

or 

git fetch
git merge origin/master
